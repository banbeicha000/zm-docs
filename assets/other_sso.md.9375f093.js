import{_ as s,o,c as a,X as e}from"./chunks/framework.a911dc49.js";const y=JSON.parse('{"title":"SSO","description":"","frontmatter":{},"headers":[],"relativePath":"other/sso.md","filePath":"other/sso.md","lastUpdated":1752670551000}'),n={name:"other/sso.md"},l=e(`<h1 id="sso" tabindex="-1">SSO <a class="header-anchor" href="#sso" aria-label="Permalink to &quot;SSO&quot;">​</a></h1><h2 id="请求头配置" tabindex="-1">请求头配置 <a class="header-anchor" href="#请求头配置" aria-label="Permalink to &quot;请求头配置&quot;">​</a></h2><h3 id="✅-标准但常用于业务控制的请求头" tabindex="-1">✅ 标准但常用于业务控制的请求头 <a class="header-anchor" href="#✅-标准但常用于业务控制的请求头" aria-label="Permalink to &quot;✅ 标准但常用于业务控制的请求头&quot;">​</a></h3><p>这些虽然是标准的，但被大量用于自定义逻辑：</p><table><thead><tr><th>请求头</th><th>用途</th></tr></thead><tbody><tr><td><code>Authorization</code></td><td>用于携带 token（如 Bearer token、JWT 等）进行身份验证</td></tr><tr><td><code>Content-Type</code></td><td>指定请求体格式，如 <code>application/json</code>、<code>multipart/form-data</code></td></tr><tr><td><code>Accept</code></td><td>指定客户端接受的响应类型，如 <code>application/json</code>，后端可根据此返回对应格式</td></tr><tr><td><code>Origin</code></td><td>指定请求来源，用于 CORS 判断</td></tr><tr><td><code>Referer</code></td><td>指定请求来源 URL，安全审计、防盗链等用到</td></tr><tr><td><code>User-Agent</code></td><td>指定客户端信息，可用于设备判断或接口分流（如 PC / 移动）</td></tr><tr><td><code>Accept-Language</code></td><td>指定语言偏好，常用于国际化返回（如 <code>zh-CN</code>、<code>en-US</code>）</td></tr></tbody></table><h3 id="🔧-非标准但常用于业务场景的请求头-自定义" tabindex="-1">🔧 非标准但常用于业务场景的请求头（自定义） <a class="header-anchor" href="#🔧-非标准但常用于业务场景的请求头-自定义" aria-label="Permalink to &quot;🔧 非标准但常用于业务场景的请求头（自定义）&quot;">​</a></h3><p>这些是非标准（non-standard），需要客户端和后端约定好。大多以 X- 或直接业务前缀命名：</p><table><thead><tr><th>请求头</th><th>常见值</th><th>用途说明</th></tr></thead><tbody><tr><td><code>X-Requested-With</code></td><td><code>XMLHttpRequest</code></td><td>标识为 Ajax 请求，防止返回重定向</td></tr><tr><td><code>X-Auth-Token</code></td><td>JWT / Token</td><td>身份认证用，替代 <code>Authorization</code>（更灵活）</td></tr><tr><td><code>X-Session-Id</code></td><td>UUID</td><td>标识用户 Session ID，微服务下传递登录态</td></tr><tr><td><code>X-Tenant-Id</code></td><td>企业 ID</td><td>多租户系统中区分租户</td></tr><tr><td><code>X-Trace-Id</code></td><td>UUID</td><td>链路追踪 ID，日志和 APM 系统中常用</td></tr><tr><td><code>X-Client-Version</code></td><td><code>1.0.2</code></td><td>前端应用版本号，用于灰度、兼容性控制</td></tr><tr><td><code>X-Platform</code></td><td><code>web</code> / <code>ios</code> / <code>android</code></td><td>标识客户端平台，适配不同返回逻辑</td></tr><tr><td><code>X-Timezone</code></td><td><code>Asia/Shanghai</code></td><td>客户端时区，辅助时间显示</td></tr><tr><td><code>X-Env</code></td><td><code>dev</code> / <code>prod</code></td><td>区分环境，用于接口代理或调试辅助</td></tr></tbody></table><p>🛡️ 注意点</p><ul><li>非标准头建议以 X- 开头，避免与标准头冲突。</li><li>现代一些建议移除 X- 前缀，直接使用自定义名称，比如：Tenant-Id、Trace-Id，但必须后端允许。</li><li>浏览器有些头是受保护的（forbidden headers），不能直接修改，例如 Host、User-Agent，除非使用某些特殊方式（如代理中间层）。</li></ul><p>📌 示例：Axios 中设置常用头部</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">axios</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">get</span><span style="color:#A6ACCD;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">/api/user</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#F07178;">headers</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">&#39;</span><span style="color:#F07178;">Authorization</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">\`</span><span style="color:#C3E88D;">Bearer </span><span style="color:#89DDFF;">\${</span><span style="color:#A6ACCD;">token</span><span style="color:#89DDFF;">}\`</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">&#39;</span><span style="color:#F07178;">X-Requested-With</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">XMLHttpRequest</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">&#39;</span><span style="color:#F07178;">X-Tenant-Id</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">tenant-123</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">&#39;</span><span style="color:#F07178;">X-Trace-Id</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">uuid</span><span style="color:#A6ACCD;">()</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">&#39;</span><span style="color:#F07178;">X-Client-Version</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">1.2.3</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">&#39;</span><span style="color:#F07178;">X-Platform</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">web</span><span style="color:#89DDFF;">&#39;</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#89DDFF;">}</span></span>
<span class="line"><span style="color:#89DDFF;">}</span><span style="color:#A6ACCD;">)</span><span style="color:#89DDFF;">;</span></span></code></pre></div><h2 id="🔍-x-requested-with-xmlhttprequest-是什么" tabindex="-1">🔍 X-Requested-With: XMLHttpRequest 是什么？ <a class="header-anchor" href="#🔍-x-requested-with-xmlhttprequest-是什么" aria-label="Permalink to &quot;🔍 X-Requested-With: XMLHttpRequest 是什么？&quot;">​</a></h2><p>这是一个非标准请求头，由许多 JavaScript 库（如 jQuery）默认发送，常用于告诉后端该请求是通过 Ajax 发出的，而不是浏览器直接访问页面发出的。 ✅ 它的用途和作用：</p><ol><li>💡 后端判断请求来源</li></ol><h4 id="后端可以通过这个头来判断请求是否来自异步请求-从而决定返回方式" tabindex="-1">后端可以通过这个头来判断请求是否来自异步请求，从而决定返回方式： <a class="header-anchor" href="#后端可以通过这个头来判断请求是否来自异步请求-从而决定返回方式" aria-label="Permalink to &quot;后端可以通过这个头来判断请求是否来自异步请求，从而决定返回方式：&quot;">​</a></h4><ul><li><p>如果是普通页面请求：返回重定向（302）</p></li><li><p>如果是 Ajax 请求：返回 JSON 错误（如 401、200）</p></li></ul><ol start="2"><li>🚫 阻止浏览器自动跳转</li></ol><h4 id="虽然这个头本身不会阻止-302-行为-但后端可以根据它避免返回-302-重定向-从而间接让前端避免跳转。" tabindex="-1">虽然这个头本身不会阻止 302 行为，但后端可以根据它避免返回 302 重定向，从而间接让前端避免跳转。 <a class="header-anchor" href="#虽然这个头本身不会阻止-302-行为-但后端可以根据它避免返回-302-重定向-从而间接让前端避免跳转。" aria-label="Permalink to &quot;虽然这个头本身不会阻止 302 行为，但后端可以根据它避免返回 302 重定向，从而间接让前端避免跳转。&quot;">​</a></h4><h2 id="前端如何处理认证失败-401-时-能否获取后端返回的响应内容" tabindex="-1">前端如何处理认证失败（401）时，能否获取后端返回的响应内容 <a class="header-anchor" href="#前端如何处理认证失败-401-时-能否获取后端返回的响应内容" aria-label="Permalink to &quot;前端如何处理认证失败（401）时，能否获取后端返回的响应内容&quot;">​</a></h2><p>📦 假设后端返回：</p><div class="language-http"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">HTTP/1.1 401 Unauthorized</span></span>
<span class="line"><span style="color:#F07178;">Content-Type</span><span style="color:#F78C6C;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">application/json</span></span>
<span class="line"></span>
<span class="line"><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C792EA;">code</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">401</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;">[sso.md](../../../../WeChat%20Files/wxid_6s0vitp90u6922/FileStorage/File/2025-07/sso.md)</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C792EA;">message</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">请先登录</span><span style="color:#89DDFF;">&quot;</span></span>
<span class="line"><span style="color:#89DDFF;">}</span></span></code></pre></div><p>⚠️ 但注意以下几种会拿不到响应内容的情况：</p><h4 id="❌-情况一-后端返回-401-但没有响应体" tabindex="-1">❌ 情况一：后端返回 401，但没有响应体 <a class="header-anchor" href="#❌-情况一-后端返回-401-但没有响应体" aria-label="Permalink to &quot;❌ 情况一：后端返回 401，但没有响应体&quot;">​</a></h4><div class="language-http"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">HTTP/1.1 401 Unauthorized</span></span>
<span class="line"><span style="color:#F07178;">Content-Length</span><span style="color:#F78C6C;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">0</span></span></code></pre></div><p>➡️ error.response.data 是空的</p><h4 id="❌-情况二-浏览器拦截跨域请求的响应体-cors-没配好" tabindex="-1">❌ 情况二：浏览器拦截跨域请求的响应体（CORS 没配好） <a class="header-anchor" href="#❌-情况二-浏览器拦截跨域请求的响应体-cors-没配好" aria-label="Permalink to &quot;❌ 情况二：浏览器拦截跨域请求的响应体（CORS 没配好）&quot;">​</a></h4><p>如果接口是跨域的，后端返回了 401，但 没有设置 Access-Control-Allow-Origin，或没有包含 Access-Control-Allow-Credentials，浏览器出于安全策略：</p><p><code>拦截了响应，Axios 拿不到 response 对象，只能拿到 error.message 或 undefined。</code></p><h4 id="✅-后端需要确保跨域时正确返回-cors-头" tabindex="-1">✅ 后端需要确保跨域时正确返回 CORS 头： <a class="header-anchor" href="#✅-后端需要确保跨域时正确返回-cors-头" aria-label="Permalink to &quot;✅ 后端需要确保跨域时正确返回 CORS 头：&quot;">​</a></h4><div class="language-http"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#F07178;">Access-Control-Allow-Origin</span><span style="color:#F78C6C;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">https://your-frontend.com</span></span>
<span class="line"><span style="color:#F07178;">Access-Control-Allow-Credentials</span><span style="color:#F78C6C;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">true</span></span></code></pre></div><h2 id="统一登录分析" tabindex="-1">统一登录分析 <a class="header-anchor" href="#统一登录分析" aria-label="Permalink to &quot;统一登录分析&quot;">​</a></h2><div class="language-markdown"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">---</span></span>
<span class="line"><span style="color:#F07178;">title</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">深入解析一种经典的后端驱动SSO认证流程</span></span>
<span class="line"><span style="color:#F07178;">description</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">记录并分析一个基于Ticket和302重定向的单点登录（SSO）方案，探讨其工作原理、优缺点以及关键的安全考量。</span></span>
<span class="line"><span style="color:#A6ACCD;">---</span></span>
<span class="line"></span>
<span class="line"><span style="color:#89DDFF;"># </span><span style="color:#FFCB6B;">深入解析一种经典的后端驱动SSO认证流程</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD;">在现代前端项目中，接入公司的统一认证平台（UAP/SSO）是一个常见需求。本文将详细记录并分析一种非常经典且优雅的认证方案：它完全由后端驱动，前端应用对认证过程几乎无感知，实现了业务逻辑与认证逻辑的完美解耦。</span></span>
<span class="line"></span>
<span class="line"><span style="color:#89DDFF;">## </span><span style="color:#FFCB6B;">场景设定</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD;">首先，我们明确一下故事中的几个主角：</span></span>
<span class="line"></span>
<span class="line"><span style="color:#89DDFF;">-</span><span style="color:#A6ACCD;">   </span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#F07178;font-weight:bold;">前端应用 (FE):</span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">\`</span><span style="color:#C3E88D;">http://project.example.com</span><span style="color:#89DDFF;">\`</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">-</span><span style="color:#A6ACCD;">   一个现代单页面应用（SPA），使用 </span><span style="color:#89DDFF;">\`</span><span style="color:#C3E88D;">axios</span><span style="color:#89DDFF;">\`</span><span style="color:#A6ACCD;"> 进行网络请求。</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">-</span><span style="color:#A6ACCD;">   在请求头中会携带 </span><span style="color:#89DDFF;">\`</span><span style="color:#C3E88D;">X-Requested-With: XMLHttpRequest</span><span style="color:#89DDFF;">\`</span><span style="color:#A6ACCD;">。</span></span>
<span class="line"><span style="color:#89DDFF;">-</span><span style="color:#A6ACCD;">   </span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#F07178;font-weight:bold;">项目后台 (BE):</span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">\`</span><span style="color:#C3E88D;">http://api.project.example.com</span><span style="color:#89DDFF;">\`</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">-</span><span style="color:#A6ACCD;">   为前端提供数据接口的服务。</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">-</span><span style="color:#A6ACCD;">   与前端应用属于</span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#F07178;font-weight:bold;">同主域</span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#A6ACCD;"> (</span><span style="color:#89DDFF;">\`</span><span style="color:#C3E88D;">.example.com</span><span style="color:#89DDFF;">\`</span><span style="color:#A6ACCD;">)，这是实现无缝 Cookie 共享的关键。</span></span>
<span class="line"><span style="color:#89DDFF;">-</span><span style="color:#A6ACCD;">   </span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#F07178;font-weight:bold;">统一认证平台 (UAP):</span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">\`</span><span style="color:#C3E88D;">http://sso.example-auth.com</span><span style="color:#89DDFF;">\`</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">-</span><span style="color:#A6ACCD;">   负责处理用户登录、验证凭据、生成票据（Ticket）的独立服务。</span></span>
<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">-</span><span style="color:#A6ACCD;">   与我们的项目</span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#F07178;font-weight:bold;">跨域</span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#A6ACCD;">。</span></span>
<span class="line"></span>
<span class="line"><span style="color:#FF9CAC;font-style:italic;">&gt;</span><span style="color:#89DDFF;font-style:italic;"> </span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#F07178;font-weight:bold;">注意：</span><span style="color:#89DDFF;font-weight:bold;">**</span><span style="color:#89DDFF;font-style:italic;"> 以上所有服务均使用 </span><span style="color:#89DDFF;font-style:italic;">\`</span><span style="color:#C3E88D;font-style:italic;">http</span><span style="color:#89DDFF;font-style:italic;">\`</span><span style="color:#89DDFF;font-style:italic;"> 协议，这构成了我们后续讨论安全问题时的核心。</span></span>
<span class="line"></span>
<span class="line"><span style="color:#89DDFF;">## </span><span style="color:#FFCB6B;">核心流程详解</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD;">整个认证流程如同一场精心编排的“重定向之舞”，前端应用在此过程中非常“被动”，只需静静等待最终的结果即可。</span></span>
<span class="line"></span>
<span class="line"><span style="color:#89DDFF;">### </span><span style="color:#FFCB6B;">流程图解</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD;">使用 Mermaid 序列图可以清晰地展示各个角色之间的交互：</span></span>
<span class="line"></span>
<span class="line"><span style="color:#C3E88D;">\`\`\`</span><span style="color:#A6ACCD90;">mermaid</span></span>
<span class="line"><span style="color:#A6ACCD90;">sequenceDiagram</span></span>
<span class="line"><span style="color:#A6ACCD90;">    participant Browser as 浏览器</span></span>
<span class="line"><span style="color:#A6ACCD90;">    participant FE as 前端应用 (project.example.com)</span></span>
<span class="line"><span style="color:#A6ACCD90;">    participant BE as 项目后台 (api.project.example.com)</span></span>
<span class="line"><span style="color:#A6ACCD90;">    participant UAP as 统一认证平台 (sso.example-auth.com)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD90;">    Note over Browser, UAP: 初始状态：用户未登录</span></span>
<span class="line"><span style="color:#A6ACCD90;">    Browser-&gt;&gt;FE: 1. 访问受保护页面</span></span>
<span class="line"><span style="color:#A6ACCD90;">    activate FE</span></span>
<span class="line"><span style="color:#A6ACCD90;">    FE-&gt;&gt;BE: 2. 发起API请求 (e.g., /user/info)</span></span>
<span class="line"><span style="color:#A6ACCD90;">    deactivate FE</span></span>
<span class="line"><span style="color:#A6ACCD90;">    activate BE</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD90;">    BE--&gt;&gt;BE: 3. 检查无会话Cookie，判断未登录</span></span>
<span class="line"><span style="color:#A6ACCD90;">    BE--&gt;&gt;Browser: 4. 返回 302 重定向至UAP&lt;br/&gt;Location: sso.example.com?service=api.../callback</span></span>
<span class="line"><span style="color:#A6ACCD90;">    deactivate BE</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD90;">    Browser-&gt;&gt;UAP: 5. 遵循302，请求UAP登录页</span></span>
<span class="line"><span style="color:#A6ACCD90;">    activate UAP</span></span>
<span class="line"><span style="color:#A6ACCD90;">    Note over Browser, UAP: 用户在UAP输入用户名密码</span></span>
<span class="line"><span style="color:#A6ACCD90;">    UAP--&gt;&gt;UAP: 6. 验证凭据，生成一次性Ticket</span></span>
<span class="line"><span style="color:#A6ACCD90;">    UAP--&gt;&gt;Browser: 7. 返回 302 重定向至后台回调接口&lt;br/&gt;Location: api.../callback?ticket=...</span></span>
<span class="line"><span style="color:#A6ACCD90;">    deactivate UAP</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD90;">    Browser-&gt;&gt;BE: 8. 遵循302，请求后台回调接口(携带Ticket)</span></span>
<span class="line"><span style="color:#A6ACCD90;">    activate BE</span></span>
<span class="line"><span style="color:#A6ACCD90;">    BE-&gt;&gt;UAP: 9. [服务端] 验证Ticket有效性</span></span>
<span class="line"><span style="color:#A6ACCD90;">    activate UAP</span></span>
<span class="line"><span style="color:#A6ACCD90;">    UAP--&gt;&gt;BE: 10. Ticket有效，返回用户信息</span></span>
<span class="line"><span style="color:#A6ACCD90;">    deactivate UAP</span></span>
<span class="line"><span style="color:#A6ACCD90;">    BE--&gt;&gt;BE: 11. 创建本地会话，生成session_id</span></span>
<span class="line"><span style="color:#A6ACCD90;">    BE--&gt;&gt;Browser: 12. 返回 302 重定向至前端页面&lt;br/&gt;Header: Set-Cookie: session_id=...&lt;br/&gt;Location: project.example.com/dashboard</span></span>
<span class="line"><span style="color:#A6ACCD90;">    deactivate BE</span></span>
<span class="line"></span>
<span class="line"><span style="color:#A6ACCD90;">    Browser-&gt;&gt;FE: 13. 遵循302，访问前端目标页 (此时已携带Cookie)</span></span>
<span class="line"><span style="color:#A6ACCD90;">    activate FE</span></span>
<span class="line"><span style="color:#A6ACCD90;">    FE-&gt;&gt;BE: 14. 再次发起API请求 (自动携带Cookie)</span></span>
<span class="line"><span style="color:#A6ACCD90;">    deactivate FE</span></span>
<span class="line"><span style="color:#A6ACCD90;">    activate BE</span></span>
<span class="line"><span style="color:#A6ACCD90;">    BE--&gt;&gt;BE: 15. 验证Cookie有效</span></span>
<span class="line"><span style="color:#A6ACCD90;">    BE--&gt;&gt;FE: 16. 返回 200 OK 和业务数据</span></span>
<span class="line"><span style="color:#A6ACCD90;">    deactivate BE</span></span></code></pre></div><h3 id="分步解析" tabindex="-1">分步解析 <a class="header-anchor" href="#分步解析" aria-label="Permalink to &quot;分步解析&quot;">​</a></h3><ol><li><p><strong>访问受保护资源</strong> 用户打开前端应用的一个需要登录的页面（如 <code>/dashboard</code>）。前端应用加载后，立即向后端 <code>api.project.example.com/user/info</code> 发起数据请求。</p></li><li><p><strong>后端拦截并重定向</strong> 后端收到请求，通过检查请求头中是否包含有效的会话 Cookie（例如 <code>session_id</code>）来判断用户是否登录。发现未登录，后端不返回 <code>401</code> 错误，而是直接返回一个 <strong><code>HTTP 302 Found</code></strong> 响应。</p><div class="language-http"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">HTTP/1.1 302 Found</span></span>
<span class="line"><span style="color:#F07178;">Location</span><span style="color:#F78C6C;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">http://sso.example-auth.com/login?service=http://api.project.example.com/auth/sso-callback</span></span></code></pre></div><ul><li><strong><code>Location</code></strong> 指向 UAP 的登录页。</li><li><strong><code>service</code></strong> 参数是关键，它告诉 UAP 认证成功后应该将用户带回哪里。注意，这里设置的是<strong>后端的回调接口</strong>，而不是前端页面。</li></ul></li><li><p><strong>用户在 UAP 登录</strong> 浏览器接收到 <code>302</code> 响应后，会立即中断当前的 AJAX 请求，并将整个页面跳转到 UAP。用户在此页面完成登录操作。</p></li><li><p><strong>UAP 颁发 Ticket 并重定向回后端</strong> UAP 验证用户成功后，会生成一个一次性、短时效的 <code>ticket</code>，然后再次发起 <code>302</code> 重定向，将浏览器导向第 2 步中 <code>service</code> 参数指定的地址。</p><div class="language-http"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">HTTP/1.1 302 Found</span></span>
<span class="line"><span style="color:#F07178;">Location</span><span style="color:#F78C6C;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">http://api.project.example.com/auth/sso-callback?ticket=ST-123456-abcdefg</span></span></code></pre></div></li><li><p><strong>后端验证 Ticket，创建会话并设置 Cookie</strong> 浏览器现在请求了我们的后端回调接口，并附上了 <code>ticket</code>。后端执行以下关键操作：</p><ul><li><strong>获取 Ticket</strong>：从 URL 参数中解析出 <code>ticket</code>。</li><li><strong>验证 Ticket</strong>：向 UAP 发起一个<strong>服务端到服务端</strong>的请求，验证该 <code>ticket</code> 是否有效。这是为了防止伪造 Ticket。</li><li><strong>创建会话</strong>：UAP 确认 <code>ticket</code> 有效后，后端为该用户创建自己的会话（Session），并生成一个唯一的 <code>session_id</code>。</li><li><strong>最终重定向</strong>：后端再次返回一个 <strong><code>HTTP 302</code></strong> 响应，这次它将把用户带回真正的<strong>前端页面</strong>。</li></ul><div class="language-http"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">HTTP/1.1 302 Found</span></span>
<span class="line"><span style="color:#F07178;">Location</span><span style="color:#F78C6C;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">http://project.example.com/dashboard</span></span>
<span class="line"><span style="color:#F07178;">Set-Cookie</span><span style="color:#F78C6C;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">session_id=xyz-abc-123; Path=/; Domain=.example.com; HttpOnly</span></span></code></pre></div><p>这个响应是整个流程的点睛之笔：</p><ul><li><strong><code>Location</code></strong>：指示浏览器跳转回最初想访问的前端页面。</li><li><strong><code>Set-Cookie</code></strong>：在重定向前，将后端的会话凭证 <code>session_id</code> 写入浏览器。由于 <code>Domain=.example.com</code> 的设置，这个 Cookie 对 <code>project.example.com</code> 和 <code>api.project.example.com</code> 均有效。</li></ul></li><li><p><strong>登录成功，一切恢复正常</strong> 浏览器存储好 Cookie，然后跳转到前端页面。页面加载后再次发起 <code>API</code> 请求，此时请求头会自动携带上 <code>session_id</code> Cookie。后端验证此 Cookie 有效，返回 <code>200 OK</code> 和业务数据，流程闭环。</p></li></ol><h2 id="关键技术点分析" tabindex="-1">关键技术点分析 <a class="header-anchor" href="#关键技术点分析" aria-label="Permalink to &quot;关键技术点分析&quot;">​</a></h2><h3 id="后端驱动的优势" tabindex="-1">后端驱动的优势 <a class="header-anchor" href="#后端驱动的优势" aria-label="Permalink to &quot;后端驱动的优势&quot;">​</a></h3><p>这种模式最大的优点是<strong>前端逻辑极简</strong>。前端开发者无需关心任何认证细节（如何跳转、如何处理 Ticket），认证逻辑对前端是完全透明的。所有复杂的认证、会话管理都集中在后端，权责分明，易于维护。</p><h3 id="x-requested-with-xmlhttprequest-的角色" tabindex="-1"><code>X-Requested-With: XMLHttpRequest</code> 的角色 <a class="header-anchor" href="#x-requested-with-xmlhttprequest-的角色" aria-label="Permalink to &quot;\`X-Requested-With: XMLHttpRequest\` 的角色&quot;">​</a></h3><p>这个请求头是 <code>axios</code> 等库在发送 AJAX 请求时默认添加的，用于标识这是一个异步请求。在我们的场景中，后端选择对所有未登录请求都返回 <code>302</code>。但在其他架构中，后端可以利用此头进行区分：</p><ul><li><strong>普通页面请求</strong>（无此头）：返回 <code>302</code>。</li><li><strong>AJAX 请求</strong>（有此头）：返回 <code>401 Unauthorized</code> 和一个包含登录地址的 JSON 体，由前端的 <code>axios</code> 拦截器捕获并处理跳转。</li></ul><p>显然，当前方案选择了更简单直接的“一视同仁”策略。</p><h2 id="重大安全风险与改进建议" tabindex="-1">重大安全风险与改进建议 <a class="header-anchor" href="#重大安全风险与改进建议" aria-label="Permalink to &quot;重大安全风险与改进建议&quot;">​</a></h2><p>当前流程在逻辑上是完美的，但在实现上存在致命的安全漏洞。</p><h3 id="_1-http-明文传输-头号风险" tabindex="-1">1. HTTP 明文传输：头号风险！ <a class="header-anchor" href="#_1-http-明文传输-头号风险" aria-label="Permalink to &quot;1. HTTP 明文传输：头号风险！&quot;">​</a></h3><div class="danger custom-block"><p class="custom-block-title">致命风险</p><p>整个流程（UAP登录、Ticket传输、Cookie设置与传输）都发生在 <strong>HTTP</strong> 上。这意味着所有数据都是<strong>明文</strong>的。任何网络中间人（如不安全的公共 Wi-Fi）都可以轻易嗅探到：</p><ul><li>用户的登录密码。</li><li>URL 中的 <code>ticket</code>。</li><li>响应头中的 <code>session_id</code> Cookie。</li></ul><p>攻击者一旦窃取 <code>session_id</code>，就可以<strong>完美冒充用户</strong>，进行会话劫持。</p></div><p><strong>解决方案：</strong><strong>必须、一定、务必为所有服务（FE、BE、UAP）启用 HTTPS！</strong> 这是现代 Web 应用的基石，可以加密所有通信，从根本上解决窃听问题。</p><h3 id="_2-cookie-安全属性缺失" tabindex="-1">2. Cookie 安全属性缺失 <a class="header-anchor" href="#_2-cookie-安全属性缺失" aria-label="Permalink to &quot;2. Cookie 安全属性缺失&quot;">​</a></h3><p>后端的 <code>Set-Cookie</code> 响应头需要更加健壮，以抵御常见的 Web 攻击。</p><ul><li><strong><code>HttpOnly</code></strong>：<strong>强烈建议</strong>开启。它能防止客户端 JavaScript 读取 Cookie，有效防御 XSS 攻击窃取会话。</li><li><strong><code>Secure</code></strong>：<strong>必须</strong>在启用 HTTPS 后添加。它确保 Cookie 只通过加密的 HTTPS 连接传输，防止在混合内容（HTTP/HTTPS）的网站中泄露。</li><li><strong><code>SameSite</code></strong>：用于防御 CSRF 攻击。 <ul><li><code>SameSite=Lax</code> (推荐)：在跨站导航（如点击链接）时允许发送 Cookie，但在跨站的 <code>POST</code> 请求或 <code>iframe</code> 中则会禁止。对于此场景是很好的平衡。</li><li><code>SameSite=Strict</code>：最严格，完全禁止任何跨站情况下的 Cookie 发送。</li></ul></li></ul><p>一个更安全的 <code>Set-Cookie</code> 头应该像这样：</p><div class="language-http"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#F07178;">Set-Cookie</span><span style="color:#F78C6C;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#C3E88D;">session_id=...; Path=/; Domain=.example.com; HttpOnly; Secure; SameSite=Lax</span></span></code></pre></div><h2 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结&quot;">​</a></h2><p>我们分析的后端驱动SSO认证流程是一个非常成熟、可靠的架构设计。它将认证的复杂性从前端剥离，实现了清晰的职责分离。</p><ul><li><strong>优点</strong>：前端无感、逻辑集中、流程清晰。</li><li><strong>核心</strong>：巧妙利用 <code>302</code> 重定向和同域 <code>Set-Cookie</code> 完成认证闭环。</li></ul><p>然而，其安全性完全依赖于底层的通信协议和正确的配置。<strong>安全永远是第一位的，请立即将您的项目迁移到全站 HTTPS！</strong></p><h2 id="常见问题" tabindex="-1">常见问题： <a class="header-anchor" href="#常见问题" aria-label="Permalink to &quot;常见问题：&quot;">​</a></h2><h2 id="_1-为什么在无痕模式下看不到-302" tabindex="-1">1.为什么在无痕模式下看不到 302？ <a class="header-anchor" href="#_1-为什么在无痕模式下看不到-302" aria-label="Permalink to &quot;1.为什么在无痕模式下看不到 302？&quot;">​</a></h2><p>这是一个非常敏锐的观察。有时在测试时，我们可能会发现，虽然认证流程（跳转到登录页再跳回）正常工作，但在 Chrome DevTools 的 Network 面板中（即使勾选了 <code>Preserve log</code>），我们却看不到预期的 <code>302</code> 状态码。</p><p><strong>最核心的原因是：统一认证平台（UAP）上已经存在一个有效的登录会话了。</strong></p><p>无痕模式（Incognito Mode）只保证在窗口关闭时清除所有数据，但在窗口的生命周期内，它和普通浏览器窗口一样会保持会话和 Cookie。</p><h3 id="快速通道-认证流程" tabindex="-1">“快速通道”认证流程 <a class="header-anchor" href="#快速通道-认证流程" aria-label="Permalink to &quot;“快速通道”认证流程&quot;">​</a></h3><p>如果你的浏览器在 UAP 的域 (<code>sso.example-auth.com</code>) 下已经有了会话 Cookie（例如，在同一个无痕窗口中登录过其他系统），那么认证流程会走一条“快速通道”：</p><ol><li>你的项目后台依然返回第一个 <code>302</code>，将你导向 UAP。</li><li>浏览器请求 UAP 时，<strong>自动带上了 UAP 的会话 Cookie</strong>。</li><li>UAP 服务器识别出你已登录，<strong>于是跳过了登录页面</strong>。</li><li>UAP 立即生成 <code>ticket</code> 并返回第二个 <code>302</code>，将你导向项目的后端回调接口。</li><li>后续流程照常进行。</li></ol><p>整个过程（<code>项目 -&gt; UAP -&gt; 项目</code>）因为没有人工输入环节，发生得极快，可能在几百毫秒内完成。在 Network 面板中，这个快速的重定向链可能看起来像是一次简单的页面刷新，中间的 <code>302</code> 请求很容易被忽略或被浏览器内部缓存优化掉。</p><h3 id="如何强制看到完整的-302-链" tabindex="-1">如何强制看到完整的 302 链？ <a class="header-anchor" href="#如何强制看到完整的-302-链" aria-label="Permalink to &quot;如何强制看到完整的 302 链？&quot;">​</a></h3><p>如果你想进行干净的调试，确保看到每一步，请按以下步骤操作：</p><ol><li><strong>打开一个全新的无痕窗口</strong>，不要复用旧的。</li><li><strong>手动清除 UAP 会话</strong>： <ul><li>方法一：直接访问 UAP 的地址并点击“退出登录”。</li><li>方法二：在 DevTools 的 <code>Application &gt; Cookies</code> 面板中，找到 <code>sso.example-auth.com</code> 域，右键点击并选择 <code>Clear</code>。</li></ul></li><li>确保 Network 面板的 <code>Preserve log</code> 已勾选。</li><li>再次访问你的项目地址。</li></ol><p>完成以上步骤后，由于 UAP 中已无会话，它将被迫展示登录页面，你就能在 Network 面板中清晰地捕获到每一个 <code>302</code> 重定向请求了。</p><h2 id="_2-为什么拿到了cookie仍会重定向到认证平台-然后免登录直接刷新回当前页面" tabindex="-1">2.为什么拿到了cookie仍会重定向到认证平台，然后免登录直接刷新回当前页面 <a class="header-anchor" href="#_2-为什么拿到了cookie仍会重定向到认证平台-然后免登录直接刷新回当前页面" aria-label="Permalink to &quot;2.为什么拿到了cookie仍会重定向到认证平台，然后免登录直接刷新回当前页面&quot;">​</a></h2><h4 id="问题场景分解" tabindex="-1">问题场景分解 <a class="header-anchor" href="#问题场景分解" aria-label="Permalink to &quot;问题场景分解&quot;">​</a></h4><ol><li><strong>首次访问/Session过期</strong>: 用户访问你的前端应用（比如 <code>app.com</code>）。</li><li><strong>重定向到认证中心</strong>: 前端应用发现用户未登录，将页面重定向到统一认证平台（比如 <code>sso.com</code>）。</li><li><strong>用户登录</strong>: 用户在 <code>sso.com</code> 输入账号密码成功登录。</li><li><strong>重定向回应用</strong>: 认证平台 <code>sso.com</code> 将用户重定向回你的前端应用 <code>app.com</code>，并在 URL 中通常会携带一个 <code>ticket</code> 或 <code>code</code>。</li><li><strong>前端获取凭证</strong>: <ul><li>前端拿到 <code>ticket</code> 后，调用后端接口（比如 <code>/api/login</code>）换取真正的会话凭证（Session/Cookie 或 JWT Token）。</li><li>后端验证 <code>ticket</code> 成功，在响应头中通过 <code>Set-Cookie</code> 指令，在浏览器中种下一个用于身份认证的 Cookie（比如 <code>SESSIONID</code>），这个 Cookie 的域（Domain）通常是你的后端 API 所在的域。</li></ul></li><li><strong>正常使用</strong>: 用户在应用内操作，所有对后端 API 的请求，浏览器都会自动带上这个 <code>SESSIONID</code> Cookie。后端通过验证这个 Cookie 来确认用户身份，接口正常返回数据。</li><li><strong>问题触发点</strong>: 用户执行某个特定操作，触发了一个<strong>特殊的 API 请求</strong>。</li><li><strong>请求失败</strong>: 这个特殊的 API 请求<strong>没有携带</strong> <code>SESSIONID</code> Cookie。</li><li><strong>后端鉴权失败并重定向</strong>: 后端 API 的鉴权中间件发现这个请求没有有效凭证，认为用户未登录。根据 SSO（单点登录）的逻辑，它不会直接返回 <code>401 Unauthorized</code>，而是返回一个 <code>302 Found</code> 重定向响应，目标地址是当初配置的认证平台登录页 <code>sso.com</code>。</li><li><strong>“无感”的二次认证</strong>: <ul><li>浏览器收到 <code>302</code> 响应，自动跳转到 <code>sso.com</code>。</li><li>由于用户刚才已经在 <code>sso.com</code> 登录过了，并且 <code>sso.com</code> 自身的会话仍然有效，所以认证平台根本不需要用户再次输入密码。它直接验证通过，并立即生成一个新的 <code>ticket</code>，再次重定向回你的前端应用 <code>app.com</code>。</li></ul></li><li><strong>页面刷新</strong>: 浏览器最终被重定向回 <code>app.com</code>，并且 URL 上带着新的 <code>ticket</code>。这在用户看来，就是<strong>当前页面突然被刷新了一下</strong>。刷新后，流程回到第 5 步，重新建立了会话，后续操作又恢复正常，直到再次触发那个有问题的 API 请求。</li></ol><h3 id="核心原因分析-为什么那个接口没带-cookie" tabindex="-1">核心原因分析：为什么那个接口没带 Cookie？ <a class="header-anchor" href="#核心原因分析-为什么那个接口没带-cookie" aria-label="Permalink to &quot;核心原因分析：为什么那个接口没带 Cookie？&quot;">​</a></h3><p>整个问题的根源在于<strong>第 8 步</strong>。一个正常运行的应用中，突然有一个请求不带 Cookie，这通常不是偶然的。以下是几个最常见的原因，你需要逐一排查：</p><h4 id="_1-api-请求的跨域-cross-origin-配置问题-最常见" tabindex="-1">1. API 请求的跨域（Cross-Origin）配置问题 (最常见) <a class="header-anchor" href="#_1-api-请求的跨域-cross-origin-配置问题-最常见" aria-label="Permalink to &quot;1. API 请求的跨域（Cross-Origin）配置问题 (最常见)&quot;">​</a></h4><p>这是最可能的原因。如果你的前端和后端 API 不在同一个源（协议、域名、端口号有一个不同），那么这就是跨域请求。</p><ul><li><strong>问题</strong>: 对于跨域请求，浏览器默认是<strong>不会</strong>发送 Cookie 的。</li><li><strong>解决方案</strong>: 你需要在发起请求时，明确告知浏览器需要携带凭证。 <ul><li><strong>如果使用 <code>axios</code></strong>: 需要在 <code>axios</code> 实例或单次请求中设置 <code>withCredentials: true</code>。<div class="language-javascript"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#676E95;font-style:italic;">// 全局配置</span></span>
<span class="line"><span style="color:#A6ACCD;">axios</span><span style="color:#89DDFF;">.</span><span style="color:#A6ACCD;">defaults</span><span style="color:#89DDFF;">.</span><span style="color:#A6ACCD;">withCredentials </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#FF9CAC;">true</span><span style="color:#89DDFF;">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#676E95;font-style:italic;">// 或者单次请求配置</span></span>
<span class="line"><span style="color:#A6ACCD;">axios</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">get</span><span style="color:#A6ACCD;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">https://api.example.com/special-data</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#F07178;">withCredentials</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#FF9CAC;">true</span></span>
<span class="line"><span style="color:#89DDFF;">}</span><span style="color:#A6ACCD;">)</span><span style="color:#89DDFF;">;</span></span></code></pre></div></li><li><strong>如果使用原生 <code>fetch</code></strong>: 需要在 <code>fetch</code> 的 <code>options</code> 对象中设置 <code>credentials: &#39;include&#39;</code>。<div class="language-javascript"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#82AAFF;">fetch</span><span style="color:#A6ACCD;">(</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">https://api.example.com/special-data</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#F07178;">credentials</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">include</span><span style="color:#89DDFF;">&#39;</span><span style="color:#A6ACCD;"> </span><span style="color:#676E95;font-style:italic;">// 或 &#39;same-origin&#39;</span></span>
<span class="line"><span style="color:#89DDFF;">}</span><span style="color:#A6ACCD;">)</span><span style="color:#89DDFF;">;</span></span></code></pre></div></li></ul></li><li><strong>排查方向</strong>: 检查那个出问题的 API 请求代码，是不是它没有配置 <code>withCredentials</code> 或 <code>credentials</code>，而其他正常的请求都配置了？也许是你项目里封装了多个请求方法，而这个操作恰好调用了一个未正确封装的。</li></ul><h4 id="_2-cookie-的作用域-domain-和-path-不匹配" tabindex="-1">2. Cookie 的作用域（Domain 和 Path）不匹配 <a class="header-anchor" href="#_2-cookie-的作用域-domain-和-path-不匹配" aria-label="Permalink to &quot;2. Cookie 的作用域（Domain 和 Path）不匹配&quot;">​</a></h4><p>Cookie 本身是有作用域的。如果请求的 URL 不在 Cookie 的作用域内，浏览器也不会发送它。</p><ul><li><strong>问题</strong>: <ul><li><strong>Domain</strong>: 后端设置的 Cookie 可能指定了 <code>Domain=.api.com</code>，但那个有问题的请求发往了 <code>special.api.com</code> 或者一个完全不同的域名。</li><li><strong>Path</strong>: Cookie 可能指定了 <code>Path=/api</code>，但那个有问题的请求发往了 <code>/other-api/data</code>。</li></ul></li><li><strong>排查方向</strong>: <ol><li>打开浏览器开发者工具 (F12)。</li><li>切换到 &quot;Application&quot; (或 &quot;存储&quot;) -&gt; &quot;Cookies&quot;。</li><li>找到你的会话 Cookie（比如 <code>SESSIONID</code>），查看它的 <code>Domain</code> 和 <code>Path</code> 字段。</li><li>切换到 &quot;Network&quot; (或 &quot;网络&quot;) 面板，找到那个失败的请求，查看它的 Request URL。</li><li>对比请求的 URL 和 Cookie 的 <code>Domain</code>、<code>Path</code> 是否匹配。</li></ol></li></ul><h4 id="_3-请求的-url-协议-http-vs-https-不匹配" tabindex="-1">3. 请求的 URL 协议（HTTP vs HTTPS）不匹配 <a class="header-anchor" href="#_3-请求的-url-协议-http-vs-https-不匹配" aria-label="Permalink to &quot;3. 请求的 URL 协议（HTTP vs HTTPS）不匹配&quot;">​</a></h4><p>如果 Cookie 被设置了 <code>Secure</code> 属性，那么它只能通过 HTTPS 协议发送。</p><ul><li><strong>问题</strong>: 你的网站是 <code>https://app.com</code>，后端 API 也都是 <code>https://api.com</code>，但那个出问题的 API 请求被错误地配置成了 <code>http://api.com/special-data</code>。</li><li><strong>排查方向</strong>: 在 &quot;Network&quot; 面板中检查失败请求的 URL，确认其协议是 <code>https</code>。</li></ul><h4 id="_4-特殊的请求方式或第三方库" tabindex="-1">4. 特殊的请求方式或第三方库 <a class="header-anchor" href="#_4-特殊的请求方式或第三方库" aria-label="Permalink to &quot;4. 特殊的请求方式或第三方库&quot;">​</a></h4><ul><li><strong>问题</strong>: 这个请求可能不是由你常用的 <code>axios</code> 实例发起的。例如，它可能是一个文件上传组件、一个图表库或者其他第三方 JS 库内部发起的请求。这些库可能有自己的网络请求配置，并且默认不携带 Cookie。</li><li><strong>排查方向</strong>: 确定是哪个组件或哪段代码触发了这个 API 调用。如果是第三方库，查阅其文档，看如何配置请求以携带凭证。</li></ul><h5 id="实际项目中出现第2中情况" tabindex="-1">实际项目中出现第2中情况 <a class="header-anchor" href="#实际项目中出现第2中情况" aria-label="Permalink to &quot;实际项目中出现第2中情况&quot;">​</a></h5><ol><li><strong>初始状态</strong>: 用户登录后，可能最先与 <code>/zk-admin-management</code> 或 <code>/zk-knowledge-qa-search</code> 服务交互。这些服务在返回响应时，通过 <code>Set-Cookie</code> 指令种下了会话 Cookie。</li><li><strong>Path 的默认行为</strong>: 如果后端在设置 Cookie 时没有显式指定 <code>Path</code> 属性，浏览器或服务器框架会默认使用<strong>当前请求的 URL 路径</strong>作为 Cookie 的 <code>Path</code>。 <ul><li>例如，如果设置 Cookie 的请求是 <code>POST /zk-admin-management/login</code>，那么 Cookie 的 <code>Path</code> 就会被默认设置为 <code>/zk-admin-management</code>。</li></ul></li><li><strong>问题触发</strong>: 当你的前端应用需要调用一个新服务下的接口，比如 <code>/zk-document-processing/api/some-action</code> 时： <ul><li>浏览器检查本地的 Cookie。它发现只有 <code>Path</code> 为 <code>/zk-admin-management</code> 和 <code>/zk-knowledge-qa-search</code> 的 Cookie。</li><li>由于请求的路径 <code>/zk-document-processing/...</code> 与现有 Cookie 的 <code>Path</code> 都不匹配，浏览器<strong>不会</strong>在这次请求中携带任何一个会话 Cookie。</li></ul></li><li><strong>重定向循环</strong>: 后端的 <code>/zk-document-processing</code> 服务收到了一个没有 Cookie 的请求，认为用户未登录，于是触发了重定向到 SSO 的流程，这就解释了你看到的现象。</li><li><strong>“再次操作后才会有这个Path”</strong>: 这个重定向循环走完一遍后，最终会有一个请求（可能是认证回调请求）被 <code>/zk-document-processing</code> 服务处理，并成功设置了一个新的、<code>Path</code> 为 <code>/zk-document-processing</code> 的 Cookie。此时，你再操作调用这个服务下的接口就正常了，因为现在有匹配的 Cookie 了。</li></ol><h3 id="根源" tabindex="-1">根源 <a class="header-anchor" href="#根源" aria-label="Permalink to &quot;根源&quot;">​</a></h3><p>问题的根源在于：<strong>后端各个服务在设置身份认证 Cookie 时，没有统一 <code>Path</code> 属性，导致 Cookie 变成了“服务专属”的，无法在所有服务间共享。</strong></p><h3 id="解决方案" tabindex="-1">解决方案 <a class="header-anchor" href="#解决方案" aria-label="Permalink to &quot;解决方案&quot;">​</a></h3><p>这是一个典型的后端配置问题。前端虽然有“绕过”的办法，但既不优雅也不治本。最佳实践是在后端进行修复。</p><h4 id="最佳解决方案-后端统一设置-cookie-的-path-为根路径-推荐" tabindex="-1"><strong>最佳解决方案：后端统一设置 Cookie 的 Path 为根路径 <code>/</code> (推荐)</strong> <a class="header-anchor" href="#最佳解决方案-后端统一设置-cookie-的-path-为根路径-推荐" aria-label="Permalink to &quot;**最佳解决方案：后端统一设置 Cookie 的 Path 为根路径 \`/\` (推荐)**&quot;">​</a></h4><p>这是最正确、最一劳永逸的解决方案。</p><p><strong>你需要联系后端开发人员</strong>，让他们在所有设置会话 Cookie 的地方（登录接口、SSO回调处理接口等），显式地将 Cookie 的 <code>Path</code> 属性设置为根路径 <code>/</code>。</p><p>一个 <code>Path=/</code> 的 Cookie 意味着，浏览器会向该域名下的<strong>所有路径</strong>发送这个 Cookie，无论请求的是 <code>/zk-admin-management</code>、<code>/zk-knowledge-qa-search</code> 还是 <code>/zk-document-processing</code>。</p><p><strong>优点</strong>:</p><ul><li>一处修改，永久解决。</li><li>前端代码无需任何改动。</li><li>符合微服务或多模块应用下共享会话的标准实践。</li><li>系统逻辑更清晰、更健壮。</li></ul><h4 id="备选方案-前端在调用前-预热-会话-不推荐" tabindex="-1">备选方案：前端在调用前“预热”会话 (不推荐) <a class="header-anchor" href="#备选方案-前端在调用前-预热-会话-不推荐" aria-label="Permalink to &quot;备选方案：前端在调用前“预热”会话 (不推荐)&quot;">​</a></h4><p>如果后端由于某些原因（比如历史包袱、权限问题）暂时无法修改，前端可以采用一种“hack”的方式来规避。</p><p><strong>思路</strong>: 在第一次调用 <code>/zk-document-processing</code> 下的任何业务接口之前，先主动向该服务路径下发送一个“无害”的初始化请求（比如 <code>GET /zk-document-processing/api/health</code> 或专门的 <code>/zk-document-processing/api/init-session</code> 接口）。</p><p>这个请求会因为没有 Cookie 而触发 SSO 重定向，并最终带回一个 <code>Path</code> 为 <code>/zk-document-processing</code> 的新 Cookie。等这个“预热”流程完成后，再执行真正的业务接口调用。</p><p><strong>缺点</strong>:</p><ul><li><strong>治标不治本</strong>: 只是一个创可贴，未来新增 <code>/zk-new-service</code> 路径时，问题会再次出现。</li><li><strong>增加复杂性</strong>: 前端需要维护一个状态，来判断哪些服务的会话已经被“预热”。</li><li><strong>性能损耗</strong>: 增加了额外的网络请求和一次不必要的重定向，用户在首次操作时会感到明显的延迟或页面闪烁。</li><li><strong>体验差</strong>: 逻辑上非常奇怪，难以维护。</li></ul>`,103),t=[l];function p(c,r,i,d,C,D){return o(),a("div",null,t)}const A=s(n,[["render",p]]);export{y as __pageData,A as default};
